{
  "github": {
    "release": true,
    "assets": ["pkg/*.zip"]
  },
  "buildCommand":
    "npm run build; mkdir -p pkg; cp -r dist pkg/pocket-datasource; cd pkg; zip -r pocket-datasource-${version}.zip pocket-datasource;"
}
